export class Grid {
    numCols: number
    numRows: number

    constructor(numCols: number, numRows: number) {
        this.numRows = numRows
        this.numCols = numCols
    }

    public length() {
        return
    }
}
